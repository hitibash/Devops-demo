# This file, `secrets.yaml`, contains sensitive configuration data.
# It should be renamed to `secrets.yaml` from its template name (e.g., `secrets.example.yaml`)
# before deploying the application.
#
# IMPORTANT SECURITY NOTICE:
# This file, or any file containing actual credentials (like a `.env` file),
# MUST NEVER be committed to version control systems (e.g., Git repositories).
# Ensure it's excluded by your `.gitignore` or equivalent configuration.

apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
stringData:
  # WARNING: These are DUMMY CREDENTIALS for demonstration purposes only.
  # For production environments, always use strong, unique, and randomly generated passwords.
  MYSQL_ROOT_PASSWORD: 123456
  MYSQL_DATABASE: todo_app
  MYSQL_USER: app_user       
  MYSQL_PASSWORD: 123456     